var MKT_V6_DATA_HUB_ID='1FXpoBO658ldbr4V8wCKo0luHU3_kqHwYzAnWijA6lBM';
function v6Sheet_(name){return SpreadsheetApp.openById(MKT_V6_DATA_HUB_ID).getSheetByName(name);}
function v6Rows_(name){var s=v6Sheet_(name),v=s?s.getDataRange().getValues():[];if(v.length<2)return [];var h=v.shift();return v.map(function(r){var o={};h.forEach(function(k,i){o[k]=r[i];});return o;});}
function v6Opportunities_(payload){return {opportunities:v6Rows_('MKT_OPPORTUNITIES'),source:'DGL_MARKETING_DATA_HUB'};}
function v6OpportunitySummary_(){var rows=v6Rows_('MKT_OPPORTUNITIES'),byType={},detected=0,suppressed=0,last='';rows.forEach(function(row){var type=String(row.opportunityType||'UNKNOWN'),isSuppressed=String(row.eligibilityStatus||'').toUpperCase()==='SUPPRESSED';byType[type]=(byType[type]||0)+1;detected+=Number(row.detectedAccounts||1);suppressed+=isSuppressed?Number(row.suppressedAccounts||1):Number(row.suppressedAccounts||0);if(String(row.lastEngineRun||'')>last)last=String(row.lastEngineRun);});return {total:rows.length,detected:detected,suppressed:suppressed,byType:byType,lastEngineRun:last||null};}
function v6RunOpportunityEngine_(){var sync=v6RefreshOpportunitiesFromReports_(),opportunities=v6Rows_('MKT_OPPORTUNITIES'),pipeline=v6PipelineSyncSignals_({opportunities:opportunities});return {status:'REPORT_SOURCE_SYNCED',sync:sync,pipeline:pipeline,opportunities:opportunities,summary:v6OpportunitySummary_()};}
