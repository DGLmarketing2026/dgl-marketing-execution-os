function v6Append_(name,record){var t=v6TableHeaders_(name);t.sheet.appendRow(t.headers.map(function(k){return record[k]==null?'':record[k];}));return {status:'RECORDED'};}
function v6CopyUsage_(payload){var p=payload||{},cutoff=new Date(v6Now_().getTime()-90*86400000),usage=v6Rows_('MKT_COPY_USAGE').filter(function(x){var d=v6DateValue_(x.usedAt||x.createdAt||x.timestamp);return d&&d>=cutoff&&(!p.accountId||String(x.accountId)===String(p.accountId));}),duplicate=!!(p.accountId&&p.fingerprint&&usage.some(function(x){return String(x.accountId)===String(p.accountId)&&String(x.fingerprint)===String(p.fingerprint);}));return {status:duplicate?'SAME COPY RECENTLY USED':'CLEAR',eligible:!duplicate,usageCount90d:usage.length,usage:usage.map(function(x){return {fingerprint:x.fingerprint,campaignType:x.campaignType,usedAt:x.usedAt||x.createdAt||x.timestamp};})};}
function v6RecordCopyUsage_(payload){var p=Object.assign({},payload||{},{usedAt:new Date().toISOString()});if(!p.accountId||!p.fingerprint)throw new Error('accountId AND fingerprint REQUIRED');var status=v6CopyUsage_(p);if(!status.eligible)return status;return v6Append_('MKT_COPY_USAGE',p);}
