{
  "suite": "Retention V1",
  "runner": "node <test-file>.test.js (no test framework/package.json in this repo; each *.test.js is a standalone Node script using assert + vm, matching the existing repo convention)",
  "newTests": [
    {
      "file": "tests/v6-retention-am-activity.test.js",
      "cases": [
        {
          "name": "base detection, WITH a CUENTAS (AM Intelligence) match and no contradictions -> DETECTED",
          "guarantee": "normal-case automation: a Retention signal with AM Intelligence context and no AM-activity red flags is never suppressed by default"
        },
        {
          "name": "Sin dueno / house account -> SUPPRESSED / OWNER REQUIRED (regression), CUENTAS branch never touched",
          "guarantee": "suppression: pre-existing owner-required rule still wins first and short-circuits before any new join logic runs"
        },
        {
          "name": "cross-priority suppression (QNB beats Retention for the same account) -> HIGHER PRIORITY SIGNAL",
          "guarantee": "suppression: shared v6ApplyPrioritySuppression_ still enforces one active family per account after adding the CUENTAS join"
        },
        {
          "name": "bucket confirms genuine absence of management, no contradiction -> DETECTED",
          "guarantee": "normal-case automation: AM-activity evidence that clearly confirms no management does not block automated retention outreach"
        },
        {
          "name": "ambiguous bucket ('8. GESTIONADAS SIN OPERAR') -> SUPPRESSED / AM ACTIVITY REVIEW REQUIRED, and v6PipelineWinner_/v6PipelineSyncSignals_ route it to CLOSED / SUPPRESSED like any other suppressed row",
          "guarantee": "suppression + duplicate prevention: an ambiguous AM-activity signal never leaks into the active pipeline through the idempotent bulk pipeline sync"
        },
        {
          "name": "bucket says 'no management' but Tipo gestion says COMERCIAL (contradiction) -> SUPPRESSED / AM ACTIVITY REVIEW REQUIRED",
          "guarantee": "suppression: contradictory AM-activity evidence is flagged for human review instead of silently auto-detected or auto-suppressed"
        },
        {
          "name": "row with no CUENTAS (AM Intelligence) match at all -> SUPPRESSED / AM CONTEXT REQUIRED, join coverage metric reflects partial match",
          "guarantee": "canonical architecture: AURA never advances a NOVA-only signal to DETECTED by skipping AM Intelligence; a missing AM Intelligence record fails closed, and join coverage remains an observability metric on top of that gate, never a substitute for it"
        }
      ]
    },
    {
      "file": "tests/v6-response-events.test.js",
      "cases": [
        {
          "name": "REPLY -> pipeline RESPONDED, nextAction STOP ACCOUNT AUTOMATION / V5.5 HANDOFF, handoffStatus PENDING",
          "guarantee": "stop + handoff: a real commercial reply stops account automation and creates an AM handoff via the existing pipeline stage machine"
        },
        {
          "name": "RFQ -> pipeline RFQ RECEIVED with rfqAt set",
          "guarantee": "response capture correctly advances pipeline stage for qualifying commercial activity"
        },
        {
          "name": "BOUNCE -> MKT_CONTACTS_SECURE emailStatus INVALID via read-merge-upsert, historical fields preserved, pipeline untouched",
          "guarantee": "duplicate prevention / idempotent UPSERT (merge-by-contactId, never blind overwrite) and isolation (a delivery failure does not affect account pipeline state)"
        },
        {
          "name": "UNSUBSCRIBE -> MKT_EXCLUSIONS ACTIVE exclusion with deterministic exclusionId, pipeline untouched",
          "guarantee": "suppression / DNC governance: an unsubscribe becomes a durable, idempotent exclusion (re-processing the same event does not duplicate the exclusion row) without altering pipeline stage"
        },
        {
          "name": "CLICK / OTHER / unrecognized eventType -> action IGNORED, zero writes to any table",
          "guarantee": "normal-case automation: non-qualifying engagement events never trigger a stop, handoff, exclusion, or contact mutation"
        }
      ]
    },
    {
      "file": "tests/v6-commercial-outcomes.test.js",
      "cases": [
        {
          "name": "resolves accountId via MKT_ACCOUNTS crosswalk and writes attributedRevenue exactly equal to Monto",
          "guarantee": "attribution: revenue is copied as-is from the authoritative source, never recalculated, never summed with unrelated figures"
        },
        {
          "name": "no unique MKT_ACCOUNTS match (unknown account) -> no pipeline write, unresolvedCount increments",
          "guarantee": "never join by account name without an authoritative crosswalk; fail-closed on ambiguous/absent identity instead of guessing"
        },
        {
          "name": "account present by name but missing salesforceAccountId/externalAccountId -> unresolved",
          "guarantee": "canonical Salesforce Account ID or documented 1:1 mapping is mandatory before attribution writes anything"
        },
        {
          "name": "existing pipeline record already has a campaignId and is past CAMPAIGN ACTIVE -> nextAction marks MULTI-CAMPAIGN CONCURRENCY, campaignId is not overwritten",
          "guarantee": "attribution ambiguity is surfaced explicitly instead of silently reassigning campaign credit"
        }
      ]
    },
    {
      "file": "tests/v6-canonical-identity.test.js",
      "cases": [
        {
          "name": "externalSystem='SALESFORCE_EXPORT' with a populated externalAccountId resolves salesforceAccountId and canonicalSalesforceIdStatus:'RESOLVED'",
          "guarantee": "Canonical ID Bridge: the real, confirmed bug (exact-match 'SALESFORCE' gate silently leaving salesforceAccountId empty for real 'SALESFORCE_EXPORT' extracts) is fixed and reproduced exactly as reported"
        },
        {
          "name": "account/contact with no externalAccountId/externalContactId stays canonicalSalesforceIdStatus:'UNRESOLVED', never fabricated",
          "guarantee": "canonical Salesforce ID or a documented 1:1 mapping only; a missing identity is held, never guessed"
        },
        {
          "name": "same SALESFORCE_EXPORT resolution/non-resolution pair for salesforceContactId",
          "guarantee": "the fix applies identically to accounts and contacts, one shared helper, no divergent second code path"
        },
        {
          "name": "non-Salesforce source (e.g. MANUAL_UPLOAD) never populates salesforceAccountId (regression: /SALESFORCE/ substring match must not become match-anything)",
          "guarantee": "the more permissive substring match does not weaken the gate into accepting arbitrary sources"
        },
        {
          "name": "v6AuraAuditCanonicalIds_ returns safe RESOLVED/UNRESOLVED/MISSING aggregate counts only, no accountName/email",
          "guarantee": "AURA never surfaces PII through a read-only audit; MISSING (field never existed on the row) is distinguished from UNRESOLVED (field exists, explicitly unresolved)"
        }
      ]
    },
    {
      "file": "tests/v6-data-freshness.test.js",
      "cases": [
        {
          "name": "report source last modified 1h ago -> FRESH",
          "guarantee": "a healthy, recently-refreshed source never blocks a Retention cycle"
        },
        {
          "name": "report source last modified 10h ago -> STALE",
          "guarantee": "fail-closed: AURA must not run a Retention campaign cycle against commercial data older than the six-hour cycle that consumes it"
        },
        {
          "name": "just under the 6-hour threshold (5.9h) -> still FRESH",
          "guarantee": "the threshold is the same six-hour constant as the existing opportunity-refresh trigger, not a new invented number, and the comparison is strictly greater-than"
        },
        {
          "name": "DriveApp.getFileById is called with the real MKT_V6_REPORT_SOURCE_ID, not a fabricated id",
          "guarantee": "the freshness check inspects the actual private report source, reusing the existing DriveApp dependency already used by MarketingV6DriveArchive.gs"
        }
      ]
    },
    {
      "file": "tests/v6-retention-report.test.js",
      "cases": [
        {
          "name": "v6AuraRetentionDryRun_ STALE short-circuits before any report read",
          "guarantee": "the pilot dry run inherits the same fail-closed freshness gate as evaluate; a stale-data pilot number is never produced"
        },
        {
          "name": "v6AuraRetentionDryRun_ FRESH classifies synthetic accounts into every named category (detected, suppressedByAmActivity, suppressedByMissingAmContext, suppressedByDataQuality, reviewRequired, frequencyBlocked via the existing v6FrequencyStatus_, eligible, campaignScopesGenerated, recipientResolutionBlocked with an explicit un-guessed reason)",
          "guarantee": "the pilot reuses v6AuraEvaluateRetention_ and v6FrequencyStatus_ verbatim (no duplicated suppression/frequency logic) and never invents a category outside the exact named set"
        },
        {
          "name": "v6AuraDecisionFor_ — one assertion per branch across all 10 named decisions, plus 2 precedence-ordering checks",
          "guarantee": "a pure, side-effect-free mapper always resolves to exactly one of the ten named decisions in the specified precedence order; a real pipeline stage always outranks a stale opportunity-level suppression label"
        },
        {
          "name": "v6AuraGenerateAmCsvReport_ produces exactly the specified 30 columns, joins strictly by accountId, contains no email/phone, and lands in a distinct Drive folder from MKT_V6_ARCHIVE",
          "guarantee": "AM-facing reporting never leaks contact PII and never joins by account name; campaign-execution archives and AM reports remain in separate private folders"
        },
        {
          "name": "v6AuraWriteRunSummary_ / v6AuraRetentionRunSummary_ round-trip; unknown runId returns NOT FOUND rather than throwing",
          "guarantee": "the persisted run summary is idempotent by runId (v6UpsertByKey_) and safely queryable without fabricating a result for an unknown run"
        },
        {
          "name": "v6AuraRunRetentionCycle_ STALE produces no CSV and no summary row",
          "guarantee": "the single scheduled orchestrator never hands AM a report generated from data it already knows is stale"
        },
        {
          "name": "v6AuraRunRetentionCycle_ FRESH produces exactly one new CSV file and one persisted summary row referencing it",
          "guarantee": "the full detect -> suppress -> scope -> AM CSV -> run summary cycle runs as one atomic, traceable unit per runId"
        }
      ]
    },
    {
      "file": "tests/v6-aura-bridge.test.js",
      "cases": [
        {
          "name": "v6AuraEvaluateRetention_ aggregates DETECTED/SUPPRESSED/reviewRequired by reason and automatically builds campaign scope only for DETECTED accounts",
          "guarantee": "detect -> suppress -> build scope runs as one automatic, no-manual-list unit; suppressed/review-required accounts are never scoped"
        },
        {
          "name": "v6AuraAutoBuildRetentionScopes_ groups by (amOwner, service), excludes suppressed rows and other opportunity families",
          "guarantee": "duplicate prevention + family isolation: a Retention scope build can never include a non-Retention account or a suppressed Retention account"
        },
        {
          "name": "v6AuraStatus_ returns safe consolidated status (pipeline stage, Retention signal, audience status once a campaign is linked); no PII fields",
          "guarantee": "AURA never surfaces accountName/email/contact PII through a status query, only aggregate/status fields"
        },
        {
          "name": "v6AuraEnsureCampaignScope_ is idempotent: re-running with an added account merges into the existing scope row, never duplicates it",
          "guarantee": "duplicate prevention: repeated scheduler runs against the same scope never create parallel scope/scope-account rows"
        },
        {
          "name": "v6AuraCreateAccountStop_ closes a fresh account to CLOSED / SUPPRESSED; an account already at QUOTED keeps that stage (preventDowngrade) while nextAction/handoffStatus still update",
          "guarantee": "account stop never erases evidence that a Quote/Load already happened, while still recording the stop/handoff intent"
        },
        {
          "name": "v6AuraCreateAmHandoff_ preserves the account's current pipeline stage and refuses to run with no existing pipeline record",
          "guarantee": "AM handoff never resets an already-advanced stage back to OPPORTUNITY DETECTED and never fabricates a pipeline context that doesn't exist"
        },
        {
          "name": "v6ClassifyResponseEvent_ accepts QUOTE/LOAD as aliases of QUOTE_SIGNAL/LOAD_SIGNAL (case-insensitive) and only sets attributedRevenue on LOAD when the raw event explicitly supplies an amount",
          "guarantee": "the exact event vocabulary requested (REPLY, RFQ, BOUNCE, UNSUBSCRIBE, CLICK, QUOTE, LOAD) is accepted without a second, divergent code path, and revenue is never fabricated on a real-time event"
        },
        {
          "name": "(added Pass 4) freshness gate: STALE source blocks v6AuraEvaluateRetention_ before any write to MKT_OPPORTUNITIES/MKT_CAMPAIGN_SCOPES; FRESH source proceeds normally",
          "guarantee": "fail-closed data freshness is enforced at the same entry point the scheduler calls, with zero side effects on STALE"
        },
        {
          "name": "(added Pass 4) running v6AuraEvaluateRetention_ twice with identical input does not grow MKT_OPPORTUNITIES (full rewrite), MKT_CAMPAIGN_SCOPES, or MKT_SCOPE_ACCOUNTS (upsert by key)",
          "guarantee": "duplicate prevention / idempotency: repeated scheduler runs against the same data never accumulate duplicate rows in any of the three tables this cycle writes"
        }
      ]
    }
  ],
  "regressionCoverage": {
    "description": "Full existing suite re-run after every change in this branch, most recently after the Canonical ID Bridge / data-freshness / Retention pilot pass (Pass 4).",
    "totalFiles": 24,
    "passed": 17,
    "failed": 7,
    "failedTestsAreProvenPreExisting": true,
    "verification": "git stash of all Retention V1 changes + re-run reproduced the identical 7 failures on main before any change in this branch, confirming they are unrelated (stale index.html cache-busting version strings and V5.5 frontend/backend-adapter assertions). Re-verified after Pass 4: same 7 files, same failure reasons (e.g. v6-report-ingestion.test.js still fails on the same stale 'marketing-opportunity-engine-v6.js?v=20260827-1' assertion; v55-validation.test.js still fails on the same 'AUTOMATION READY' vs 'NEEDS CLARIFICATION' V5.5 assertion). Pass/fail counts moved only by the 3 new test files added in this pass (v6-canonical-identity.test.js, v6-data-freshness.test.js, v6-retention-report.test.js) plus 2 new cases appended to the existing v6-aura-bridge.test.js and a mock-table update in v6-schema-migration.test.js (21 files/14 passed -> 24 files/17 passed; 7 fail is unchanged throughout, same files, same reasons).",
    "failedFiles": [
      "tests/v55-audience-bridge.test.js",
      "tests/v55-backend-adapter.test.js",
      "tests/v55-legacy-status-visibility.test.js",
      "tests/v55-validation.test.js",
      "tests/v6-live-opportunities-ui.test.js",
      "tests/v6-report-ingestion.test.js",
      "tests/v6-validation.test.js"
    ]
  }
}
