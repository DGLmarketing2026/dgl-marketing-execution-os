(function (global) {
  "use strict";
  const esc=(v)=>String(v==null?"":v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");
  async function render(container){
    const contract=global.DGL_MARKETING_AGENT_CONTRACT;
    const [queueResult,activityResult,campaignResult]=await Promise.all([contract.getAgentQueue(),contract.getAgentActivity(),contract.getCampaigns()]);
    const queue=queueResult.data||[], activity=(activityResult.data||[]).slice(0,8), campaigns=campaignResult.data||[];
    const count=(s)=>queue.filter(x=>x.status===s).length;
    container.innerHTML=`<div class="v5-workspace agent-control"><div class="v5-topbar"><div><div class="v5-kicker">AGENT ARCHITECTURE V1</div><h1>Agent Control</h1><p>Operational visibility for the future Claude Marketing Agent within Marketing governance.</p></div><div class="v5-top-actions"><span class="v5-badge green">AGENT READY / NOT CONNECTED</span></div></div>
      <div class="agent-disconnected"><i data-lucide="unplug"></i><div><strong>Agent integration not connected yet.</strong><span>This dashboard uses safe local demo records. No Claude API call or external execution occurs.</span></div></div>
      <div class="v5-grid-kpi"><div class="v5-kpi"><div class="label">READY TO PREPARE</div><div class="value">${count("READY")}</div><div class="foot">AUTO actions</div></div><div class="v5-kpi"><div class="label">WAITING APPROVAL</div><div class="value">${count("WAITING APPROVAL")}</div><div class="foot">Marketing decision required</div></div><div class="v5-kpi"><div class="label">ACTIVE AUTOMATIONS</div><div class="value">${campaigns.filter(c=>c.automationStatus==="RUNNING").length}</div><div class="foot">Eligible accounts continue</div></div><div class="v5-kpi"><div class="label">BLOCKED / NEEDS ATTENTION</div><div class="value">${count("BLOCKED")}</div><div class="foot">Clarification or policy gate</div></div></div>
      <section class="v5-panel v5-panel-pad"><div class="v5-panel-head"><div><h2>AGENT QUEUE</h2><p>Explicit actions; the future agent does not operate by clicking the UI.</p></div><span class="v5-badge">LOCAL DEMO</span></div><div class="agent-table"><div class="agent-row head"><span>Action</span><span>Campaign</span><span>AM Owner</span><span>Status</span><span>Permission</span><span>Requested At</span><span>Next Step</span></div>${queue.map(x=>`<div class="agent-row"><strong>${esc(x.action)}</strong><span>${esc(x.campaign)}</span><span>${esc(x.amOwner)}</span><span class="v5-state-pill ${x.status==="READY"||x.status==="ACTIVE"?"ready":""}">${esc(x.status)}</span><span class="permission ${x.permission==="AUTO"?"auto":"approval"}">${esc(x.permission)}</span><span>${esc(x.requestedAt)}</span><span>${esc(x.nextStep)}</span></div>`).join("")}</div></section>
      <section class="v5-panel v5-panel-pad"><div class="v5-panel-head"><div><h2>RECENT AGENT ACTIVITY</h2><p>Safe demo audit records answer what, why, where, when and who approved.</p></div></div><div class="agent-table activity"><div class="agent-row head"><span>Timestamp</span><span>Action</span><span>Campaign / Request</span><span>Permission</span><span>Status</span><span>Result</span><span>Approved By</span></div>${activity.map(x=>`<div class="agent-row"><span>${esc(x.timestamp||x.requestedAt)}</span><strong>${esc(x.agentAction||x.actionType)}</strong><span>${esc(x.campaignId||x.entityId)}${x.requestId?` / ${esc(x.requestId)}`:""}</span><span>${esc(x.permissionLevel)}</span><span>${esc(x.status)}</span><span>${esc(typeof x.result==="string"?x.result:JSON.stringify(x.result||"—"))}</span><span>${esc(x.approvedBy||"—")}</span></div>`).join("")}</div></section>
      <section class="v5-panel v5-panel-pad"><div class="v5-panel-head"><div><h2>AUTOMATION RULES</h2><p>AM decides what. Marketing and the future Claude Marketing Agent execute how.</p></div></div><div class="permission-grid"><article><span class="permission auto">AUTO</span><h3>Prepare safely</h3><p>Prepare campaigns, copy, creative, sequence, QA, analysis and recommendations.</p></article><article><span class="permission approval">APPROVAL REQUIRED</span><h3>High-impact execution</h3><p>Launch, send audience communications, resume or materially change campaigns.</p></article><article><span class="permission never">NEVER AUTO</span><h3>Commercial and sensitive controls</h3><p>Commercial decisions, exclusion overrides, pricing, credit, ownership or sensitive account changes.</p></article></div><div class="response-rule"><strong>CUSTOMER RESPONSE</strong><span>=</span><strong>STOP AUTOMATION FOR THAT ACCOUNT</strong><span>+</span><strong>HANDOFF TO AM</strong><small>Other eligible campaign accounts continue.</small></div></section></div>`;
    global.lucide?.createIcons();
  }
  global.DGL_MODULE_RENDERERS=global.DGL_MODULE_RENDERERS||{};global.DGL_MODULE_RENDERERS["agent-control"]=(container)=>{render(container).catch(err=>{container.innerHTML=`<p>Agent Control unavailable: ${esc(err.message)}</p>`})};
})(window);
