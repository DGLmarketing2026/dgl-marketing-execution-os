# DGL brand assets

Place only approved, official DGL brand files in this directory.

Campaign Studio intentionally ships without a simulated logo. The official logo
selector accepts a configured approved asset path; until one is provided, the
email preview uses a neutral `DEDICATED GROUND LOGISTICS` text fallback.
